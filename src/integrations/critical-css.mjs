// @ts-check
// Local-only stub for missing integration file (not in repo, only referenced).
// Replace with original implementation when porting to upstream.
export function criticalCSSIntegration() {
  return {
    name: "critical-css-stub",
    hooks: {},
  };
}
